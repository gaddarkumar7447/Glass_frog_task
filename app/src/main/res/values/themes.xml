<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GlassTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GlassTask" parent="Base.Theme.GlassTask" />

    <style name="CustomAlertDialogStyle" parent="Theme.MaterialComponents.Dialog.Alert">
        <item name="android:fontFamily">@font/figtree_medium</item>
        <item name="android:windowBackground">@color/taskColor</item>
        <item name="textAllCaps"/>
        <item name="android:textColor">@color/black</item>
    </style>

</resources>